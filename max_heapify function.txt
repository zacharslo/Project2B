//Max_heapify 

max_heapify(A, i){
	l = left(i);
	r = right(i);

	if(l <= A.heap.size && A[l] > A[i])
		largest = l;
		else
			largest = i;
			if(r < A.heap.size && A[r] > A[largest])
				largest = r;
			if(largest != i){
				swap(A[i].A[largest]);
				max_heapify(A.largest);
			}
}